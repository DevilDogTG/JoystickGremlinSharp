name: Smoke Test
on:
  push:
    branches: [main]
jobs:
  smoke-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ubuntu ok"
  smoke-windows:
    runs-on: windows-2022
    steps:
      - run: echo "windows ok"
