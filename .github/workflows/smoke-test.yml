name: Smoke Test
on:
  push:
    branches: [main]
jobs:
  with-actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: dotnet --version
  windows-bare:
    runs-on: windows-2022
    steps:
      - run: echo "windows ok"
  windows-with-actions:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: dotnet --version
