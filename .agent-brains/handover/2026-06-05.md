## Session Handover — 2026-06-05

### Accomplished
- **Released v12.1.0** (signed MSI on GitHub Releases) — full cycle in one session:
  change request → plan → implementation → two review rounds → merge → release.
- **Global auto-load triggers** (PR #71, ADR-0003): triggers moved from
  profile-embedded (v11 design) to a global ordered list in `settings.json`
  (`AppSettings.AutoLoadTriggers`, `ProfilePath`-referencing). Idempotent lossless
  migration (`AutoLoadTriggerMigrator`: settings-first ordering + dedupe-on-retry),
  automatic at startup + manual "Migrate now" banner on the Auto-load page. Flat
  trigger table UI with per-row profile picker. `ProfileLibrary` no longer reads
  triggers during scans (ScanCore-async backlog item closed as moot).
- **Review round 1 caught a real torn-read defect** (rows mutated trigger instances
  the monitor thread enumerates) — fixed via `ToTrigger()` snapshots; also gated
  `SettingsService.SaveAsync` with a semaphore, moved detection I/O off the UI
  thread, and fixed a user-found ComboBox selection loss on page navigation
  (SelectedItem/ItemsSource attach-order race). Round 2: Approved. 328 tests.
- **Workspace `release` skill** (PR #72): analyzes commits/PRs since last tag,
  recommends semver bump for user confirmation, writes curated `RELEASE-NOTES.md`,
  opens `release/vX.Y.Z` PR, watches `tag.yml` → `publish.yml`. publish.yml switched
  from `generate_release_notes` to `body_path: RELEASE-NOTES.md` (+ fallback step) —
  verified live by the v12.1.0 release (PR #73).
- **Process correction**: user flagged missing atomic-commit discipline (Global
  §6.5) — work was split into atomic conventional commits retroactively; saved to
  Claude auto-memory: commit-as-you-go, NO `Co-Authored-By` trailer.

### Current State
- Active plan: none — both session plans completed and archived
  (`archive/feature-global-autoload.md`, `archive/release-skill.md`).
- Branch: `main`, clean, pushed. Only `.claude/settings.local.json` modified
  (local permissions, conventionally uncommitted).
- v12.1.0 tag/release live; `version.json` = 12.1.0.
- Local branch cleanup: deleted 5 stale branches (`chore/agent-brains-profiles`,
  `chore/cleanup-hidhide-and-ci`, `chore/session-handover-2026-05-28` — note: its
  unmerged 05-28 memo commit was discarded as superseded, `develop`,
  `feature/hidhide-startup-ux`).

### Next Steps
1. Nothing in flight. Backlog (optional features): GitHub Releases version checker,
   response curve editor, condition-based action pipeline, button-mapping UI.
2. For the next release, invoke the workspace `release` skill (sk-release) — never
   tag manually (tag.yml/publish.yml own tagging + release creation).
3. Optional gap: ADR-0002 (WiX installer decision, 2026-05-28) is referenced in
   memory but its file was never written — backfill if auditability matters.

### Decisions Made
- **ADR-0003**: global auto-load trigger store reversing v11 per-profile embedding;
  path-based profile references; replace-only concurrency convention (also added as
  a workspace AGENT.md rule).
- Release-flow decisions captured in the `release` skill itself + archived plan:
  workspace-level skill, curated notes via `body_path`, fallback for noteless tags.

### Blockers / Notes
- New workspace rule: **Shared-Collection Concurrency** in `.agent-brains/AGENT.md` —
  AppSettings collections read by background threads are replace-only; row VMs
  snapshot, never write through.
- Trigger profile references break if a profile file is moved/renamed (⚠ marker in
  UI, re-point via dropdown) — accepted limitation, path-fixup is a possible future
  enhancement.
- profile `gh-release` skill remains generic/manual — unsafe for this repo; the
  workspace `release` skill documents why. (Promotion of an "automation guard" step
  to the profile skill was offered and declined this session.)
